# boilerplate_nextjs

```
Scripts

$ yarn dev # Starts the development server (HMR enabled)
$ yarn build # Creates the production build of the app
$ yarn start # Starts the production app (from build)
$ yarn type-check # Type checks with using typescript
$ yarn lint # Linting capabilities using eslint
```
